import 'package:flutter/material.dart';

const miDefaultSize = 14.0;
const miAnimationDuration = Duration(milliseconds: 200);
const miPrimaryColor = Color(0xFF388E3C);
const miLightColor = Color(0xFF4CAF50);
