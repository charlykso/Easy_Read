import 'package:flutter/material.dart';

const miDefaultSize = 14.0;
const miAnimationDuration = Duration(milliseconds: 200);
const miDarkColor = Color(0xFF388E3C);
const miPrimaryColor = Color(0xFF4CAF50);
const miLightGreyColor = Color(0x66efefef);
